# ==========================================
# Orquestia BPM Studio - Application Config
# ==========================================

spring.application.name=orquestia-backend

# Server
server.port=8080

# MongoDB
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://orquestia:orquestia2026@localhost:27017/orquestia_db?authSource=admin}

# JWT Configuration
app.jwt.secret=${APP_JWT_SECRET:OrquestiaBPMStudio2026SecretKeyForJWTTokenGenerationMustBeLongEnough256Bits!!}
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:4200}

# Logging
logging.level.com.orquestia=DEBUG
logging.level.org.springframework.security=INFO

# Dev tools
spring.devtools.restart.enabled=true

# MinIO (S3-compatible storage)
minio.endpoint=${MINIO_ENDPOINT:https://minio-wcggss4ggw4kgog0oswgsk84.62.171.144.14.sslip.io}
minio.access-key=${MINIO_ACCESS_KEY:josuecova}
minio.secret-key=${MINIO_SECRET_KEY:rodriguez2708}
minio.bucket=${MINIO_BUCKET:orquestia}
